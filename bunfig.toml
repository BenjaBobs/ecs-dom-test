[test]
# No global preload - each test creates its own Window for isolation
# Run tests in parallel for speed
# Only print failures for cleaner output
onlyFailures = true
concurrent = true
timeout = 500
