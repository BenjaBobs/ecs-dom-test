{
  "name": "ecs-test-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "bun --watch apps/playground/index.html"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}